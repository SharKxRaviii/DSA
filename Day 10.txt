title: Sort an array of 0s, 1s and 2s

Problem Statement: Given an array nums consisting of only 0, 1, or 2. Sort the array in non-decreasing order. The sorting must be done in-place, without making a copy of the original array.

Examples
Input: nums = [1, 0, 2, 1, 0]
Output: [0, 0, 1, 1, 2]
Explanation: The nums array in sorted order has 2 zeroes, 2 ones and 1 two

Input: nums = [0, 0, 1, 1, 1]
Output: [0, 0, 1, 1, 1]
Explanation: The nums array in sorted order has 2 zeroes, 3 ones and zero twos.

solution:
function sortArr (arr) {
  let zero = 0;
  let one = 0;
  let two = 0;
  
  for (let a of arr) {
    if (a === 0) zero++;
    else if (a === 1) one++;
    else two++;
  }
  
  let index = 0;
  
  while (zero--) arr[index++] = 0;
  while (one--)  arr[index++] = 1;
  while (two--)  arr[index++] = 2;

}

sortArr(nums);
console.log((nums));
